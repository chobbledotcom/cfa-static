---
meta_description:
meta_title: The News System, Demonstrated
name: The News System, Demonstrated
subtitle: Dated posts, an RSS feed, and a list of older articles
author: Jane Doe
blocks:
  - type: include
    file: news-post-header.html
  - type: news-meta
  - type: markdown
    content: |
      News posts are markdown files in `src/news/`. Each one gets its date
      from its filename or front matter, an optional **author** and
      **subtitle**, and a place in the site-wide [RSS feed](/feed.xml).

      The body of a post is the same block system as every other page - this
      one uses a `news-meta` block for the byline, a `markdown` block for the
      text you are reading, and includes for the header and gallery.

      > Blockquotes, **bold text**, lists, and code all render the way you
      > would expect, because it is just markdown.

      Posts are listed newest-first on the [news page](/news/), which uses an
      `items` block pointed at the news collection.
  - type: include
    file: news-post-gallery.html
  - type: include
    file: faq.html
---
