---
meta_description:
meta_title: A Second Post, For the Archive
name: A Second Post, For the Archive
subtitle: Two posts make a list page worth having
blocks:
  - type: include
    file: news-post-header.html
  - type: news-meta
  - type: markdown
    content: |
      A single news post looks lonely on a listing page, so here is a second
      one (see the [first post](/news/first/) for the tour of what a post can
      contain).

      This one has no `author` in its front matter, so the byline block below
      the title renders the date alone - the template treats the author as
      optional rather than inventing one.

      Delete both of these posts when you start writing real news, or delete
      the whole `src/news/` directory if the site does not need a news
      section at all.
  - type: include
    file: news-post-gallery.html
  - type: include
    file: faq.html
---
