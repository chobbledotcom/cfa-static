# @11ty/eleventy is pinned to a 4.0 prerelease, and semver ranges like the
# schema plugin's ">=0.11.0" peer requirement never match prerelease versions
# under npm's strict resolver. Bun tolerated this; npm needs the legacy
# behaviour until Eleventy 4 is stable and the pin moves off the alpha.
legacy-peer-deps=true
